[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [
    ".venv",
    "data",
    "logs",
    "dbt_packages",
    "target",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra --basetemp=.pytest_tmp"
